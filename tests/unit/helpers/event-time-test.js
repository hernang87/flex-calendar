
import { eventTime } from 'flex-calendar/helpers/event-time';
import { module, test } from 'qunit';

module('Unit | Helper | event time');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = eventTime([42]);
  assert.ok(result);
});

