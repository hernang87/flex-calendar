
import { times } from 'flex-calendar/helpers/times';
import { module, test } from 'qunit';

module('Unit | Helper | times');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = times([42]);
  assert.ok(result);
});

