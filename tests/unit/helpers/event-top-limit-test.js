
import { eventTopLimit } from 'flex-calendar/helpers/event-top-limit';
import { module, test } from 'qunit';

module('Unit | Helper | event top limit');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = eventTopLimit([42]);
  assert.ok(result);
});

