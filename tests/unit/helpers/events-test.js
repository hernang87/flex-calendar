
import { events } from 'flex-calendar/helpers/events';
import { module, test } from 'qunit';

module('Unit | Helper | events');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = events([42]);
  assert.ok(result);
});

